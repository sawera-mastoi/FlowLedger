{
  "name": "flowledger-monorepo",
  "private": true,
  "description": "Monorepo for the FlowLedger ecosystem on Stacks.",
  "scripts": {
    "dev": "npx serve packages/flowledger-dapp",
    "lint": "npm run lint --workspaces --if-present",
    "format": "npm run format --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "earnwithalee",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sawera-mastoi/FlowLedger.git"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
